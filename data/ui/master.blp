using Gtk 4.0;

template $Dock: Window {
  name: "Dock++";
  default-height: 40;

  styles [
    "dock_window"
  ]

  Box master_box {
    styles [
      "master_box"
    ]

    homogeneous: true;
    spacing: 10;
  }
}
