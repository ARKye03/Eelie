#include <gtkmm.h>

class master_window
{
private:
    /* data */
public:
    master_window(/* args */);
    ~master_window();
};

master_window::master_window(/* args */)
{
}

master_window::~master_window()
{
}
